import '@testing-library/jest-dom/vitest';
import { afterAll, afterEach, beforeAll } from 'vitest';

// Global setup for unit tests
beforeAll(() => {
	// Mock global objects if needed
});

afterEach(() => {
	// Clear mocks or resets
});

afterAll(() => {
	// Cleanup
});
